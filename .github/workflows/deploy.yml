on:
  pull_request:
    branches:
      - master    # producción
      - develop   # desarrollo

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Test deploy locally
        run: |
          echo "Conexión exitosa en el runner"
          pwd
          ls -la
          # Aquí puedes poner directamente tus comandos de despliegue
          # por ejemplo:
          # docker compose -f docker-compose.dev.yml up -d --build
